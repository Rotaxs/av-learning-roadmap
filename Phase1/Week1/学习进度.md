# 第 1 周学习进度

> 本文件由学习完成情况检查维护。`学习路线.md` 定义验收标准，本文件记录核验结果；二者冲突时以路线为准。

## 当前结论

- 最近检查：2026-08-13 17:10
- 检查范围：第 5 天（Git 过程与 GDB 定位）
- 总体结论：通过
- 放行结论：可以进入第 6 天（干净复现、验收与复盘）
- 复验限制：无（本次新空 build 目录 `/tmp/w1d5-check` configure+build+ctest 7/7 复验通过）

## 验收明细

| 范围 | 检查项 | 类型 | 状态 | 证据或缺口 |
| --- | --- | --- | --- | --- |
| 第5天 | `git status` 中没有误提交的构建产物 | 每日结束条件 | 已完成 | 已清理并复验：`4277fe6` 移除 ELF 二进制（`demo09/test`、`demo10/demo`、`demo02/stats`、`demo03/main`）并在 `.gitignore` 增加 `*.i`/`*.s`；`3ff9dad` 再次 `git rm --cached` 追踪中的 `demo02/stats.i`/`stats.s`。最终 `git ls-files` 无 ELF/中间产物，`git status` 干净 |
| 第5天 | 能用 GDB 展示至少 2 层调用栈和一个决定错误行为的变量 | 每日结束条件 | 已完成 | `evidence/gdb-session.txt`：`bt` 两帧（`process_data` at demo.cpp:5 → `main` at demo.cpp:11）；`p ptr` = `0x0` 是决定崩溃的变量 |
| 第5天 | 缺陷修复后全部测试再次通过，证据含完整定位链路 | 每日结束条件 | 已完成 | 证据链完整：复现命令 `g++ -g demo.cpp -o demo` → SIGSEGV → `bt`/`p ptr` → 根因（空指针解引用）→ 修复（空指针检查）→ 回归运行正常退出。修复在 demo10 lab 而非 textstats 项目，project 测试未受影响；本次新空 build 目录 ctest 7/7 通过 |
| 第5天 | `project/` 内 Git 历史：骨架、P0 和测试变更 | P0（当天交付） | 已完成 | `git log`：含 CMakeLists 提交、ctest 设计提交、`finish test part`、`finish day4 task` 等小步提交，commit message 描述行为变化；沿父仓库提交，无嵌套 `git init` |
| 第5天 | `evidence/gdb-session.txt`：复现命令、调用栈、变量、根因、修复与回归 | P0（当天交付） | 已完成 | 六要素齐备；未演示 `break`/`next`/`step`（本次 crash 定位未用断点），不影响结束条件#2 |
| 第5天 | `notes/baseline.md` 追加工作区/暂存区/提交解释及易错点 | P0（当天交付） | 已完成 | baseline.md“第 5 天：Git 基线”含三区域表与易错点（`git add .` 误添加、`git diff` 与 `git diff --staged` 区别） |
| 第5天 | 自测题 Q1（`git diff` 与 `git diff --staged`） | 自测 | 已完成 | baseline.md“易错点记录”回答正确 |
| 第5天 | 自测题 Q2（无 `-g` 时 GDB 定位源码为何困难） | 自测 | 已完成 | 学习者口述：丢失行号、源码、局部变量、变量类型等信息；与 `notes/gdb基础.md` 所述 `-g` 使调试器可查看源代码一致 |
| 第5天 | 自测题 Q3（调用栈、崩溃行与根因为何可能不同） | 自测 | 已完成 | `学习路线.md` 书面作答：空指针经参数传入函数后解引用，崩溃行、调用栈、根因三处不同；与本次 demo10（崩溃 `demo.cpp:5`，根因 `main` 传空指针）吻合 |

## 已验证证据

| 证据 | 验证方式 | 结果 |
| --- | --- | --- |
| 干净构建与全部测试 | 本次新空 build 目录 `/tmp/w1d5-check` | configure+build exit=0，ctest 7/7 passed |
| GDB 两层调用栈与关键变量 | 复读 `evidence/gdb-session.txt` | `bt` 两帧、`p ptr`=`(int *) 0x0`、崩溃行 `*ptr = 100` at demo.cpp:5 |
| 修复与回归 | 复读 `evidence/gdb-session.txt` + `codes/demo10/demo.cpp` | 修复点为空指针检查；回归运行打印“警告：指针为空”并正常退出 |
| Git 小步历史 | `git log --oneline` | 含 CMake/测试/修复小步提交，`docs(week1): finish day5 tasks` 为第 5 天提交 |
| 误提交构建产物（已清理） | `git ls-files` + `file` | `4277fe6` 移除 `demo09/test`/`demo10/demo`/`demo02/stats`/`demo03/main` ELF；`3ff9dad` 移除 `stats.i`/`stats.s`；复扫无 ELF/中间产物被跟踪 |
| 构建产物忽略现状 | 读 `.gitignore` | `*.i`/`*.s` 已加入忽略（`4277fe6`）；结合既有 `*.o`/`CMakeFiles/`/build 目录规则，无扩展名 ELF 也已从追踪清除 |

## 阻塞与欠账

无。

## 改进建议

无阻塞项。可留意的 Backlog：白天第二次回溯发现仓库已分支（本地 5 vs 远端 1 提交待整合），进入第 6 天前建议 `git pull`/处理 divergence，避免后续证据提交冲突。

## 待口述验证

无。三项自测题均已作答：Q1 与 Q3 已书面写入 `学习路线.md`（Q3 用空指针经参数传入函数举例，符合 demo10 的崩溃位置≠根因位置），Q2 学习者口述（丢失行号、源码、局部变量、变量类型）。

## 阶段汇总

<!-- 仅阶段检查时写入 -->

## 阶段汇总

<!-- 仅阶段检查时写入 -->

## 检查历史

| 时间 | 范围 | 结论 | 摘要 |
| --- | --- | --- | --- |
| 2026-08-09 | 第1天 | 未通过 | 工具记录已核验且有效；`notes/baseline.md` 缺失，10 项自测与薄弱项补救未完成 |
| 2026-08-10 | 第1天 | 通过 | 缺失两项已补齐：`baseline.md` 10 项评级与最弱项补救齐备；`exit_code_test.sh` 本次执行验证成功/失败退出码差异，可以进入第 2 天 |
| 2026-08-10 | 第2天 | 未通过 | 三个结束条件中两项已完成并复验（手工两对象文件链接、统计值可复算）；结束条件 #3 未满足：不存在文件错误写的是 stdout 而非 stderr（勾选框也未勾，诚实）。第 3 天 CMake 转换不受影响，但第 4 天错误场景测试前必须修复 |
| 2026-08-10 20:21 | 第2天 | 通过 | 复验：stderr 修复完成（不存在文件走 stderr+exit=1）、文件名统一为 `stats.cpp`、`stats.h` 加 `#pragma once`、失败改抛异常替代魔法值 `{0,1,1}`；三个结束条件与当天交付全部复验通过。学习者在路线自测题处作答，3 题正确（`U`/`T`、强符号多定义与 inline 弱符号、诊断排序），口述验证通过。欠账延后至第 4 天：多参数校验与“末尾无换行补一行”行数规则 |
| 2026-08-11 19:49 | 第3天 | 通过 | /tmp 复验 GCC+Clang 双编译器从空 build 目录构建均 exit=0，`flags.make` 确认 `-Wall -Wextra -Wpedantic -Werror` 生效即无警告；`CMakeCache.txt` 确认两树编译器真实不同；源码树无残留 .o，`.gitignore` 对 `build-*` 生效。三个结束条件与当天交付全部复验通过，自测 3 题书面回答正确。欠账：`.gitignore` 拼写 `bulid/`→`build/`；第 4 天须补多参数校验与末尾无换行补行规则，并更新 README“预期命令” |
| 2026-08-12 21:15 | 第4天 | 未通过 | 本次复验：新空 build 目录 configure+build exit=0，ctest 6/6 pass；六类输入行为、统计值与人工核算全部一致，多参数/末尾无换行/`.gitignore` 三项欠账均已修复。但 `README.md` 仍为“预期命令”占位且示例夹具 `simple.txt` 不存在（结束条件#3 与当天交付未完成）；“曾观察到一个预期测试失败”无仓库证据（待口述）。自测 3 题书面回答正确。放行：暂不进入第 5 天，先补 README 与失败观察证据。临时复验目录 `/tmp/w1d4-build` |
| 2026-08-12 21:27 | 第4天 | 未通过 | 学习者已补齐：README 占位移除并换用 `normal.txt`（README 命令逐条实测可执行）、新增 `cli_multi_files` 多参数自动测试、`evidence/ctest.log` 记录故意失败（`no_newline` Expected 8 vs Actual 7，ctest exit 8）。但故意改错的期望值 8 尚未恢复为 7，当前 7 测试 6 过 1 失败，也缺“恢复后重跑全部通过”的最终日志；仅剩此一步与一个 Backlog 级 README stdout 契约小缺口，完成后即可放行进入第 5 天 |
| 2026-08-12 21:30 | 第4天 | 通过 | 最终复验通过：`no_newline` 期望值恢复为 7/1/2，`main.cpp` 移除 “loading file” 行使 stdout 与 README 契约一致；`ctest.log` 同时含故意失败记录与 7/7 全部通过（ctest exit 0）；本次新空 build 目录 configure+build+ctest 7/7 复验通过，六类输入行为与人工核算一致。第 4 天全部结束条件与当天交付完成，可以进入第 5 天。遗留仅 2 项 Backlog（`docs/stdout.txt` 旧格式、测试失败打印小瑕疵） |
| 2026-08-13 16:55 | 第5天 | 未通过 | GDB 两项结束条件与全部当天交付均复验通过（本次新空 build 目录 ctest 7/7；`gdb-session.txt` 含两层调用栈、关键变量与完整定位链路）。自测题 Q1/Q3 已书面写入 `学习路线.md`（Q3 用空指针传参举例，符合 demo10 崩溃位置≠根因），Q2 口述（丢失行号、源码、局部变量、变量类型）通过。问题仅剩结束条件#1：第 5 天提交 `408a7d6` 把 `demo09/test`/`demo10/demo` ELF 及 `demo02/stats`/`demo03/main`/`stats.i`/`.s` 构建产物提交入库，`*.o` 已忽略但无扩展名二进制未覆盖。放行：暂不进入第 6 天，需清理 |
| 2026-08-13 17:10 | 第5天 | 通过 | 构建产物清理完成并复验：`4277fe6` 移除 4 个 ELF 二进制并新增 `.gitignore` 的 `*.i`/`*.s`；`3ff9dad` 将已追踪的 `demo02/stats.i`/`stats.s` 解除追踪。最终 `git status` 干净、`git ls-files` 无任何 ELF/中间产物；三项自测题均已回答。第 5 天全部结束条件与当天交付通过，可以进入第 6 天 |