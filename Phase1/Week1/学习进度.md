# 第 1 周学习进度

> 本文件由学习完成情况检查维护。`学习路线.md` 定义验收标准，本文件记录核验结果；二者冲突时以路线为准。

## 当前结论

- 最近检查：2026-08-11 19:49
- 检查范围：第 3 天（用 CMake 管理构建）
- 总体结论：通过
- 放行结论：可以进入第 4 天；第 2 天遗留的多参数校验与“末尾无换行补一行”行数规则已在欠账中并须在第 4 天补齐；`.gitignore` 拼写错误建议顺手修正
- 复验限制：无（Clang 21.1.8 已可用并实际复验）

## 验收明细

| 范围 | 检查项 | 类型 | 状态 | 证据或缺口 |
| --- | --- | --- | --- | --- |
| 第3天 | 两个全新 build 目录均构建成功 | 每日结束条件 | 已完成 | 本次 /tmp 复验：GCC 与 Clang 各从空 build 目录 configure+build 均 exit=0；`evidence/build-gcc.log`、`build-clang.log` 含完整命令、输出与退出码 |
| 第3天 | 项目自身无 `-Wall -Wextra -Wpedantic` 警告 | 每日结束条件 | 已完成 | `flags.make` 确认 GCC 与 Clang 均启用 `-Wall -Wextra -Wpedantic -Werror`，两编译器构建 exit=0，即无任何警告 |
| 第3天 | 能指出每个 target 的源码、公开头文件路径和依赖关系 | 每日结束条件 | 已完成 | `CMakeLists.txt` 结构清晰：`core`（`src/stats.cpp`，include 仅 `include/`，`cxx_std_17` PUBLIC 传导）+ `textstats`（`src/main.cpp`，PRIVATE 链接 `core`）；自测题书面回答正确 |
| 第3天 | `CMakeLists.txt`：C++17、targets、include 路径与警告选项 | P0（当天交付） | 已完成 | 用 `target_compile_features(... cxx_std_17)` 声明标准并 PUBLIC 传导，`target_sources`/`target_include_directories`/`target_compile_options`/`target_link_libraries` 齐备；子目录 `add_subdirectory` 仅见于 `codes/demo05、demo06` 实验，未带入本项目（符合降级方案） |
| 第3天 | `.gitignore`：构建目录及临时文件规则 | P0（当天交付） | 已完成 | `build-*/` 已生效：`git check-ignore` 确认 `build-gcc`/`build-clang` 被忽略，源码与证据均未忽略。注：`bulid/` 为 `build/` 的拼写错误，普通 `build/` 目录不会被忽略（本轮无此目录，见欠账） |
| 第3天 | `evidence/build-gcc.log`、`evidence/build-clang.log`：从 configure 到 build 的命令、输出和退出状态 | P0（当天交付） | 已完成 | `CMakeCache.txt` 确认 GCC 树用 `/usr/bin/g++`、Clang 树用 `/usr/bin/clang++`，与标题一致；每份日志含 configure+2 step build、`Configure/Build Exit Code: 0` |

## 已验证证据

| 证据 | 验证方式 | 结果 |
| --- | --- | --- |
| GCC/Clang 从空目录构建 | 本次 /tmp 复验（`w1d3-gcc`、`w1d3-clang`） | configure exit=0，build exit=0，均生成 `core` 静态库与 `textstats` 可执行文件 |
| 编译器选择正确性 | 读取 `CMakeCache.txt` + 日志识别行 | 两树编译器分别为 `/usr/bin/g++`、`/usr/bin/clang++`；clang.log 含 “The CXX compiler identification is Clang 21.1.8” |
| 警告选项生效 | 读取 `CMakeFiles/core.dir/flags.make` | 两树 `CXX_FLAGS = -Wall -Wextra -Wpedantic -Werror` |
| 源码树无对象文件 | `find project -name '*.o'`（排除 build-*） | 无残留 .o，out-of-source 干净 |
| `.gitignore` 生效 | `git check-ignore project/build-gcc project/build-clang` | 两者均被忽略；`git ls-files` 无构建产物 |

## 阻塞与欠账

| 优先级 | 事项 | 影响 | 处理时机 |
| --- | --- | --- | --- |
| 中 | `main.cpp` 未校验多参数：`textstats a b` 返回 0 只统计第一个文件 | 与 README P0“接受且仅接受一个文件路径参数”“多参数返回非零”不符，第 4 天错误测试缺该场景 | 第 4 天 |
| 中 | 行数规则未实现“非空末尾无换行补一行”：当前 `abc`=lines 0，README 规则应为 1 | README 与实现不一致，第 4 天“明确行数规则”时须实现并补测试 | 第 4 天 |
| 低 | `.gitignore` 拼写错误：`bulid/` 应为 `build/` | 普通 `build/` 目录若被创建将误入版本控制 | 第 4 天起随手修正 |
| 低 | `baseline.md` 第 5、6 天表格仍为“待填写” | 属预留结构，不影响第 3 天 | 第 5、6 天 |
| 低 | `README.md` 构建命令仍标为“预期命令” | 路线明确第 4 天更新 README 为真实命令，属预定期限 | 第 4 天 |

## 改进建议

| 优先级 | 建议 | 原因与证据 | 预计时间 | 类型 |
| --- | --- | --- | ---: | --- |
| 1 | 修正 `.gitignore`：`bulid/` → `build/`，保留 `build-*/` | 当前普通 `build/` 目录不会被忽略，可能把构建产物误提交；`git check-ignore` 已验证 `build-*` 生效 | 2 分钟 | 进入下一日/周前完成 |
| 2 | 第 4 天补齐第 2 天遗留：`argc > 2` 报错并 return 非零；实现 `abc` 末尾无换行补一行（lines=1） | 二者是第 4 天错误场景与边界测试的前置，且与 README P0/规则冲突 | 30 分钟 | 进入下一日/周前完成 |

## 待口述验证

无。第 3 天 3 道自测题已由学习者在本周路线自测问题处书面作答，与 `notes/cmake基础.md` 及实际配置一致：

1. 推荐 out-of-source build 的原因（一源多树、中间产物与源码分离）——正确，与 cmake 笔记及本项目双 build 目录对应。
2. `PRIVATE`/`PUBLIC` include usage requirement：PRIVATE 不被依赖者使用，PUBLIC 成为公共接口——正确，本项目 `core` 用 PUBLIC 传导 include 与 `cxx_std_17` 即按此设计。
3. 确认 CMake 用的是 Clang 而非 GCC：看 configure 输出的编译器识别、`CMakeCache.txt` 或 `--verbose` 编译命令——正确，本次已核验缓存中的编译器路径。

## 阶段汇总

<!-- 仅阶段检查时写入 -->

## 检查历史

| 时间 | 范围 | 结论 | 摘要 |
| --- | --- | --- | --- |
| 2026-08-09 | 第1天 | 未通过 | 工具记录已核验且有效；`notes/baseline.md` 缺失，10 项自测与薄弱项补救未完成 |
| 2026-08-10 | 第1天 | 通过 | 缺失两项已补齐：`baseline.md` 10 项评级与最弱项补救齐备；`exit_code_test.sh` 本次执行验证成功/失败退出码差异，可以进入第 2 天 |
| 2026-08-10 | 第2天 | 未通过 | 三个结束条件中两项已完成并复验（手工两对象文件链接、统计值可复算）；结束条件 #3 未满足：不存在文件错误写的是 stdout 而非 stderr（勾选框也未勾，诚实）。第 3 天 CMake 转换不受影响，但第 4 天错误场景测试前必须修复 |
| 2026-08-10 20:21 | 第2天 | 通过 | 复验：stderr 修复完成（不存在文件走 stderr+exit=1）、文件名统一为 `stats.cpp`、`stats.h` 加 `#pragma once`、失败改抛异常替代魔法值 `{0,1,1}`；三个结束条件与当天交付全部复验通过。学习者在路线自测题处作答，3 题正确（`U`/`T`、强符号多定义与 inline 弱符号、诊断排序），口述验证通过。欠账延后至第 4 天：多参数校验与“末尾无换行补一行”行数规则 |
| 2026-08-11 19:49 | 第3天 | 通过 | /tmp 复验 GCC+Clang 双编译器从空 build 目录构建均 exit=0，`flags.make` 确认 `-Wall -Wextra -Wpedantic -Werror` 生效即无警告；`CMakeCache.txt` 确认两树编译器真实不同；源码树无残留 .o，`.gitignore` 对 `build-*` 生效。三个结束条件与当天交付全部复验通过，自测 3 题书面回答正确。欠账：`.gitignore` 拼写 `bulid/`→`build/`；第 4 天须补多参数校验与末尾无换行补行规则，并更新 README“预期命令” |