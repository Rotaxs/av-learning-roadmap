# 第 1 周学习进度

> 本文件由学习完成情况检查维护。`学习路线.md` 定义验收标准，本文件记录核验结果；二者冲突时以路线为准。

## 当前结论

- 最近检查：2026-08-12 21:30
- 检查范围：第 4 天（补齐 P0 与自动测试）
- 总体结论：通过
- 放行结论：可以进入第 5 天（Git 过程与 GDB 定位）
- 复验限制：无（本次新空 build 目录 configure+build+ctest 7/7 复验通过）

## 验收明细

| 范围 | 检查项 | 类型 | 状态 | 证据或缺口 |
| --- | --- | --- | --- | --- |
| 第4天 | P0 六类输入都有明确行为（普通/空/无末尾换行/仅空白/缺参数/不存在） | 每日结束条件 | 已完成 | 本次 /tmp 复验：`normal`=62/6/9、`empty`=0/0/0、`no_newline`(abc def)=7/1/2、`whitespace`=8/2/0 均与人工核算一致；缺/多参数与不存在文件均 stderr 报错且 exit=1，无伪统计 |
| 第4天 | 至少 5 个测试全部通过，且曾观察到一个预期测试失败 | 每日结束条件 | 已完成 | `ctest.log` 记录故意失败（`no_newline` Expected 8 vs Actual 7，ctest exit 8）后又记录 7/7 全部通过（ctest exit 0），失败观察与恢复重跑两个证据链完整；本次新 build 目录复验 7/7 pass |
| 第4天 | README 中的命令与当前项目一致，可逐条执行 | 每日结束条件 | 已完成 | “预期命令”占位已移除，示例夹具 `normal.txt` 存在；本次按 README 在 `build-gcc` 逐条执行 configure/build/ctest/运行命令均可用，`normal` 输出 62/6/9 |
| 第4天 | `project/tests/`：测试入口与小型合法测试夹具，文件名表达场景 | P0（当天交付） | 已完成 | 7 个用例（normal/empty/no_trailing_newline/whitespace + missing_arg/not_exist/multi_files）场景清晰；夹具为可人工核算的最小自建文本 |
| 第4天 | `project/CMakeLists.txt`：注册全部测试 | P0（当天交付） | 已完成 | 7 个 `add_test`；`cli_missing_arg`/`cli_not_exist`/`cli_multi_files` 均 `WILL_FAIL TRUE`，错误路径验证退出码而非文本 |
| 第4天 | `project/README.md`：更新实际已验证的构建、运行、测试命令和统计规则 | P0（当天交付） | 已完成 | 命令为已验证版本；`main.cpp` 已移除“loading file”前缀，stdout 与 README L14 单行契约一致 |
| 第4天 | `evidence/ctest.log`：测试总数、全部通过输出和命令退出状态 | P0（当天交付） | 已完成 | 含故意失败记录（观察证据）与最终 7/7 passed + `ctest exit status 0`；本次复验结果与之吻合 |
| 欠账 | 多参数校验（第2天遗留） | 欠账 | 已完成 | `main.cpp` `argc != 2` + `cli_multi_files` 自动测试；实测 `a.txt b.txt` → usage + exit=1 |
| 欠账 | “末尾无换行补一行”行数规则（第2天遗留） | 欠账 | 已完成 | `stats.cpp` L30 补行；no_newline=1 行与人工核算一致 |
| 欠账 | `.gitignore` 拼写 `bulid/` → `build/` | 欠账 | 已完成 | 现为 `build/` + `build-*/`，`git check-ignore` 生效，无构建产物提交 |

## 已验证证据

| 证据 | 验证方式 | 结果 |
| --- | --- | --- |
| 全部测试通过 | 本次新空 build 目录 `/tmp/w1d4-pass` | configure+build exit=0，ctest 7/7 passed，exit 0 |
| 故意失败观察 | 复读 `evidence/ctest.log` 前半段 | `no_newline` Expected bytes=8 vs Actual=7、断言行与路径、`Error while running CTest`、`ctest exit status 8` |
| 恢复后重跑 | `evidence/ctest.log` 后段 + 本次复验 | 7/7 passed、`ctest exit status 0`；`no_newline` 期望值已恢复为 `7` |
| 六类输入行为 | `/tmp/w1d4-pass` 直接运行 | normal/empty/no_newline/whitespace 统计与人工核算一致；缺/多参数、不存在 exit=1 + stderr |
| README 命令一致性 | 按 README 在 `build-gcc` 逐条执行 | 均可用；stdout 现为单行 `bytes= lines= words=`，与 README L14 一致 |
| 多参数自动测试 | ctest `cli_multi_files` | 通过；CLI 实测 `a.txt b.txt` → usage + exit=1 |
| Git 状态 | `git status`/`git diff` | 待提交改动：`stats_test.cpp` 期望值 8→7（已恢复）、`main.cpp` 移除 loading 行、`CMakeLists.txt` 加多参数测试、`README.md` 命令更新、`ctest.log` 失败+通过双记录、删除未用的 `sample.txt`；无构建产物入库。第 5 天 Git 任务将处理提交 |

## 阻塞与欠账

| 优先级 | 事项 | 影响 | 处理时机 |
| --- | --- | --- | --- |
| 低 | `docs/stdout.txt` 仍记录旧的“loading file”两行输出，与 `main.cpp` 现行单行输出不符 | 文档遗留，不影响任何标准与命令 | Backlog（顺手更新） |
| 低 | `stats_test.cpp` L20 失败打印 `" words" << s.words` 缺少 `=`（显示 `words2`） | 仅影响失败时诊断可读性 | Backlog 可选 |

## 改进建议

| 优先级 | 建议 | 原因与证据 | 预计时间 | 类型 |
| --- | ---: | --- | ---: | --- |
| 1 | 顺手更新 `docs/stdout.txt` 为现行单行输出（`bytes= lines= words=`） | `main.cpp` 已移除 loading 行，该文件仍为旧的示例输出 | 2 分钟 | Backlog |

## 待口述验证

无。第 4 天两个需要动手观察的项均已形成仓库证据：`ctest.log` 同时记录了故意失败（Expected 8 vs Actual 7、exit 8）与恢复后 7/7 通过（exit 0）；第 4 天自测 3 题书面回答正确。

## 阶段汇总

<!-- 仅阶段检查时写入 -->

## 检查历史

| 时间 | 范围 | 结论 | 摘要 |
| --- | --- | --- | --- |
| 2026-08-09 | 第1天 | 未通过 | 工具记录已核验且有效；`notes/baseline.md` 缺失，10 项自测与薄弱项补救未完成 |
| 2026-08-10 | 第1天 | 通过 | 缺失两项已补齐：`baseline.md` 10 项评级与最弱项补救齐备；`exit_code_test.sh` 本次执行验证成功/失败退出码差异，可以进入第 2 天 |
| 2026-08-10 | 第2天 | 未通过 | 三个结束条件中两项已完成并复验（手工两对象文件链接、统计值可复算）；结束条件 #3 未满足：不存在文件错误写的是 stdout 而非 stderr（勾选框也未勾，诚实）。第 3 天 CMake 转换不受影响，但第 4 天错误场景测试前必须修复 |
| 2026-08-10 20:21 | 第2天 | 通过 | 复验：stderr 修复完成（不存在文件走 stderr+exit=1）、文件名统一为 `stats.cpp`、`stats.h` 加 `#pragma once`、失败改抛异常替代魔法值 `{0,1,1}`；三个结束条件与当天交付全部复验通过。学习者在路线自测题处作答，3 题正确（`U`/`T`、强符号多定义与 inline 弱符号、诊断排序），口述验证通过。欠账延后至第 4 天：多参数校验与“末尾无换行补一行”行数规则 |
| 2026-08-11 19:49 | 第3天 | 通过 | /tmp 复验 GCC+Clang 双编译器从空 build 目录构建均 exit=0，`flags.make` 确认 `-Wall -Wextra -Wpedantic -Werror` 生效即无警告；`CMakeCache.txt` 确认两树编译器真实不同；源码树无残留 .o，`.gitignore` 对 `build-*` 生效。三个结束条件与当天交付全部复验通过，自测 3 题书面回答正确。欠账：`.gitignore` 拼写 `bulid/`→`build/`；第 4 天须补多参数校验与末尾无换行补行规则，并更新 README“预期命令” |
| 2026-08-12 21:15 | 第4天 | 未通过 | 本次复验：新空 build 目录 configure+build exit=0，ctest 6/6 pass；六类输入行为、统计值与人工核算全部一致，多参数/末尾无换行/`.gitignore` 三项欠账均已修复。但 `README.md` 仍为“预期命令”占位且示例夹具 `simple.txt` 不存在（结束条件#3 与当天交付未完成）；“曾观察到一个预期测试失败”无仓库证据（待口述）。自测 3 题书面回答正确。放行：暂不进入第 5 天，先补 README 与失败观察证据。临时复验目录 `/tmp/w1d4-build` |
| 2026-08-12 21:27 | 第4天 | 未通过 | 学习者已补齐：README 占位移除并换用 `normal.txt`（README 命令逐条实测可执行）、新增 `cli_multi_files` 多参数自动测试、`evidence/ctest.log` 记录故意失败（`no_newline` Expected 8 vs Actual 7，ctest exit 8）。但故意改错的期望值 8 尚未恢复为 7，当前 7 测试 6 过 1 失败，也缺“恢复后重跑全部通过”的最终日志；仅剩此一步与一个 Backlog 级 README stdout 契约小缺口，完成后即可放行进入第 5 天 |
| 2026-08-12 21:30 | 第4天 | 通过 | 最终复验通过：`no_newline` 期望值恢复为 7/1/2，`main.cpp` 移除 “loading file” 行使 stdout 与 README 契约一致；`ctest.log` 同时含故意失败记录与 7/7 全部通过（ctest exit 0）；本次新空 build 目录 configure+build+ctest 7/7 复验通过，六类输入行为与人工核算一致。第 4 天全部结束条件与当天交付完成，可以进入第 5 天。遗留仅 2 项 Backlog（`docs/stdout.txt` 旧格式、测试失败打印小瑕疵） |